/// Stub: sin geolocalizacion del navegador fuera de web.
Future<({double latitude, double longitude})> getBrowserCurrentPosition() {
  throw UnsupportedError(
    'getBrowserCurrentPosition solo esta disponible en Flutter Web.',
  );
}
